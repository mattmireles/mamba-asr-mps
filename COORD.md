# COORD.md — session coordination ledger

Append-only, newest at the bottom, one line per substantive prompt when its work
lands: `- [YYYY-MM-DD HH:MMZ] [session-or-lane] <what was asked> -> <what landed> | evidence: <exit code / commit / path / status>`.
Honest entries only: in-progress is "in progress", untested is "untested". Compact
to COORD-ARCHIVE.md at ~40 ledger lines. In a fable-director arrangement, lane
blackboards live beside this file as COORD-<LANE>.md; this file is the ship/main ledger.

## LEDGER
- [2026-07-27 01:27Z] [hook] COORD.md scaffolded by oracle-suite SessionStart
- [2026-07-27 02:4xZ] [main] Import + adapt kokoro-coreml CLAUDE.md and skills -> 20 skills in .claude/skills/, README/{Skills,Templates,Guides,Notes,Plans} scaffolded, CLAUDE.md merged (Core ML field guide Parts 1-5 + skills index), AGENTS.md mirrored. Key adaptation: repo has NO test suite, so every pytest gate rewritten to `train_CTC.py --epochs 1 --sanity`. Skipped botnet + audio-judge (no analog). No existing files moved (scripts/report_phase3.sh reads README/training-notes.md at runtime). | evidence: `pytest --collect-only` -> "no tests collected"; sanity gate exit 0 (loss 16.86, 4.51s); link check 268/268 resolve, 0 empty dirs; 20 SKILL.md frontmatter names match dirs
- [2026-07-27 02:14Z] [main + 4 opus lanes] Deep ship-readiness assessment (user ask, ilya-sutskever) -> verdict delivered in chat: pipeline shell executes but no trained model exists anywhere; RNNT loss unusable (dead-constant branch train_RNNT.py:706, naive impl autograd bug); export path has no parity gate — random weights pass as "ready for deployment"; no .pt/.ckpt survives machine-wide, LibriSpeech absent, /Users/mattmireles legacy home locked-unsearched; best-ever accuracy CER 0.86 on synthetic audio (plan-v2:344), real WER always 1.000 | evidence: CTC sanity exit 0 (loss 42.2, MPS); RNNT sanity exit 1; random-init MambaASR_probe.mlpackage -> Swift runner exit 0 @3.17ms/chunk; lanes A/B/C/D read-only, 368k subagent tokens; no spend ledger file exists (noted here instead)
- [2026-07-27 02:30Z] [main] Update CLAUDE.md against kokoro-coreml baseline (user ask) -> kokoro baseline unchanged since 2026-07-10 so structure already current; restored dropped "Think scrappy" section; added dated "Ground truth (audited 2026-07-27)" anti-rot section (no trained model, RNNT broken w/ file:line, no parity gate, StateType absent, chunk-256 coincidence, env gaps, green gates); fixed false StateType claim in Part 2; annotated RNN-T gate row as exits-1; AGENTS.md mirrored | evidence: git diff --stat CLAUDE.md+AGENTS.md, cmp exit 0; docs-only change, sanity gate not applicable
- [2026-07-27 02:45Z] [main] create-plan (explicit invocation) -> README/Plans/v1-ship-ctc29.md written from Plans-template: 4 serial phases (P0 env+data, P1 contracts+parity-on-random-weights BEFORE training, P2 train-clean-100 to dev-clean WER<=0.25 w/ NaN root-cause + stop rule, P3 export/eval/latency/handoff), non-goals RNN-T/KD-QAT-pruning/StateType/LM/iOS-app, CI import constraint + artifact policy + anti-rot duty as hard contracts; Plans/README.md index updated (new plan active, plan-v2 marked superseded) | evidence: file exists, 4 relative links verified OK, no implementation performed, uncommitted pending git-commit/execute-plan
