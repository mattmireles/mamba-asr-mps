# Apple Silicon baseline hparams for Conformer-CTC (FP32)
model:
  d_model: 512
  n_blocks: 6
  vocab_size: 1024

train:
  batch_size: 4
  lr: 0.0003
  epochs: 10
  precision: fp32

mps:
  high_watermark_ratio: 0.8
  autocast: false
