# Apple Silicon baseline hparams for MCT RNNT (FP32)
model:
  feat_dim: 80
  d_model: 256
  n_blocks: 6
  state_dim: 16
  vocab_size: 1024
  joint_dim: 320

train:
  batch_size: 2
  lr: 0.0003
  epochs: 10
  precision: fp32

mps:
  high_watermark_ratio: 0.8
  autocast: false
